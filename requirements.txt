# BlindWatch AI – Python Backend Dependencies
# Core API framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
aiosqlite

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP client (gateway proxy)
httpx>=0.27.0

# File upload support
python-multipart>=0.0.9

# AI / Vision Pipeline (install for live YOLO processing)
# Uncomment below when GPU/CPU environment is ready:
# ultralytics>=8.2.0       # YOLOv8 + ByteTrack
# opencv-python-headless>=4.9.0
# torch>=2.2.0
# torchvision>=0.17.0

# Numpy (always required – used in synthetic frame pipeline)
numpy>=1.26.0

# Background job support
# celery[redis]  # optional: replace threading with Celery for production

# Pydantic
pydantic>=2.0.0
